package gabywald.terminal3.serverside.shell;

/**
 * proposal of Substitu for isVisible / isHidden 'Tag' for ICommand's. 
 * Command (all cmd's), inclusive (added to common), Exclusive (without common). 
 * @author Gabriel Chandesris (2026)
 */
public enum ECommandModule {
	COMMON, TYPE1_INCLUSIVE, TYPE2_EXCLUSIVE;
}
